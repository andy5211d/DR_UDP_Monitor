A utility to monitor and decode the UDP packets from Diverecorder.   

The following will be implemented when I fully understand Malcolms code!
1. Has the capability of knowing if there is more than one NIC on the machine running this software and alows the user to select which is used for the monitor function, (code is from Malcolms software).
2. Additionally the programe can scan the network to find all the instances of Diverecorder running, (code is from Malcolms software).
