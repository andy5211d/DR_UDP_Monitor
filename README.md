A utility to monitor and decode the UDP packets from Diverecorder.   

This utility is written in Delphi using Embarcadero's RAD Studio V12 IDE

The following will be implemented when I fully understand Malcolms code!
1. Has the capability of knowing if there is more than one NIC on the machine running this software and allows the user to select which is used for the monitor function, (code is from Malcolm's software).
2. Additionally the program can scan the network to find all the running instances of Diverecorder, (code is from Malcolm's software).
